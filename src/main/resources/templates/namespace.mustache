<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="context-prefix" content="{{contextPrefix}}">
    <title>Namespace Listing</title>
    <link rel="stylesheet" href="/css/lib/bootstrap.min.css">
      <link type="text/css" rel="stylesheet" href="/css/emissary.css"/>
  </head>

  <body>
    <header></header>
    <main class="container">
        <h2>Namespace Listing</h2>
        <table class="table table-striped">
           <thead>
              <tr>
                  <th>Key</th>
                  <th>Value</th>
              </tr>
            </thead>
            <tbody>
              {{#namespaces}}
              <tr class='{{clz}}'>
                {{#isDir}}
                  <td>
                    <a href="{{dumpUrl}}">{{key}}</a>
                    (<a href="{{transferUrl}}">xml</a>)
                  </td>
                  <td>
                    {{value}}({{valueClassName}})
                  </td>
                {{/isDir}}
                {{^isDir}}
                    <td>{{key}}</td>
                    <td>
                      {{value}}({{valueClassName}})
                    </td>
                {{/isDir}}
              </tr>
              {{/namespaces}}
           </tbody>
         </table>
    </main>

      <script src="/js/lib/jquery.min.js"></script>
      <script src="/js/lib/popper.min.js"></script>
      <script src="/js/lib/bootstrap.min.js"></script>
    <script src="/js/emissary.js" type="text/javascript" ></script>
  </body>
</html>
